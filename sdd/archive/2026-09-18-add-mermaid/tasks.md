# add-mermaid 任務清單

- [x] 1. npm 安裝 mermaid，以動態 import 延遲載入
- [x] 2. preview.js：mermaid 區塊輸出為待繪製元素，繪製後替換為 SVG；錯誤時顯示錯誤訊息；依原始碼快取 SVG
- [x] 3. main.js：預覽繪製（含非同步圖表）完成後重新計算同步捲動錨點
- [x] 4. 樣式與中英文錯誤字串；建置並以測試版驗收

## 驗收條件

- 情境：文件中有 ```` ```mermaid ```` 的 `graph TD; A-->B` 區塊，預覽顯示為流程圖（SVG），不是原始碼
- 情境：同一份文件中有循序圖與甘特圖，都能正確繪製
- 情境：圖表語法打錯時，該位置顯示「圖表語法錯誤」與錯誤訊息，其他內容照常顯示
- 情境：在圖表以外的地方打字，圖表不會重畫閃爍（使用快取）
- 情境：離線狀態下圖表仍能繪製（函式庫已內嵌）
- 情境：沒有圖表的文件，啟動與預覽速度不受影響（mermaid 未載入）
